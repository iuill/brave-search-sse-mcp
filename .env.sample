# このファイルをコピーして .env という名前に変更し、
# 実際の値を設定してください。
# .env ファイルは Git にコミットしないでください (.gitignore に含まれています)。

# Brave Search API (必須)
# https://brave.com/search/api/ から取得してください。
BRAVE_API_KEY=your_brave_api_key_here

# MCPサーバーがリッスンするポート番号 (任意、デフォルト: 3005)
# docker-compose.yml とサーバー内部の両方で使用されます。
PORT=3005

# Docker コンテナの再起動ポリシー (任意、デフォルト: unless-stopped)
# 有効な値: no, on-failure, always, unless-stopped
RESTART_POLICY=unless-stopped
